name: 'fivetran_oms'
version: '1.0.0'
config-version: 2

profile: 'fivetran_oms'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  fivetran_oms:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: STAR_SCHEMA
      +materialized: table
